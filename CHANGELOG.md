# Changelog

## [Unreleased]

- Scaffold inicial: auth (Devise + Google OAuth), roles cumulativas
  (`admin`/`super_admin`), multi-tenant (contas + memberships + onboarding com
  aprovação), painel `/admin` (Tailwind) e `/super_admin` (ActiveAdmin com
  pipeline de assets dart-sass para Propshaft), Docker, Railway + Cloudflare.
