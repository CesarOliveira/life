<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Life</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="min-h-screen bg-slate-50 text-slate-800 antialiased">
    <% if user_signed_in? %>
      <nav class="sticky top-0 z-20 border-b border-slate-200 bg-white/80 backdrop-blur">
        <div class="mx-auto flex h-14 max-w-5xl items-center justify-between gap-2 px-4">
          <div class="flex items-center gap-1">
            <%= link_to authenticated_root_path, class: "mr-3 flex items-center gap-2 font-semibold text-slate-900" do %>
              <span class="inline-flex h-7 w-7 items-center justify-center rounded-lg bg-emerald-500 text-sm text-white">✦</span>
              <span>Life</span>
            <% end %>
            <% if current_account %>
              <%= nav_link "Dashboard", authenticated_root_path %>
              <%= nav_link "Hábitos", habits_path %>
            <% end %>
          </div>
          <div class="flex items-center gap-2">
            <% if current_account %>
              <details class="relative">
                <summary class="flex cursor-pointer list-none select-none items-center gap-1 rounded-lg border border-slate-200 px-3 py-1.5 text-sm text-slate-600 hover:bg-slate-50">
                  <span class="font-medium text-slate-900"><%= current_account.name %></span>
                  <span class="text-slate-400">▾</span>
                </summary>
                <div class="absolute right-0 z-10 mt-2 min-w-[220px] rounded-xl border border-slate-200 bg-white py-1 shadow-lg">
                  <% current_user.active_accounts.each do |acc| %>
                    <%= button_to acc.name, switch_account_path(acc),
                          class: "block w-full px-3 py-1.5 text-left text-sm hover:bg-slate-50 #{'font-semibold text-emerald-700' if acc.id == current_account.id}" %>
                  <% end %>
                  <div class="my-1 border-t border-slate-100"></div>
                  <% if current_user.admin_of?(current_account) %>
                    <%= link_to "Membros da conta", account_members_path(current_account), class: "block px-3 py-1.5 text-sm hover:bg-slate-50" %>
                  <% end %>
                  <%= link_to "Criar nova conta", new_account_path, class: "block px-3 py-1.5 text-sm hover:bg-slate-50" %>
                  <%= link_to "Entrar em outra conta", new_join_request_path, class: "block px-3 py-1.5 text-sm hover:bg-slate-50" %>
                </div>
              </details>
            <% end %>
            <% if current_user&.platform_admin? %>
              <%= link_to "Admin", admin_root_path, class: "hidden rounded-lg px-3 py-1.5 text-sm font-medium text-slate-500 hover:bg-slate-100 hover:text-slate-900 sm:inline" %>
            <% end %>
            <% if current_user&.super_admin? %>
              <%= link_to "Super Admin", "/super_admin", class: "hidden rounded-lg px-3 py-1.5 text-sm font-medium text-slate-500 hover:bg-slate-100 hover:text-slate-900 sm:inline" %>
            <% end %>
            <%= link_to "Sair", destroy_user_session_path, data: { turbo_method: :delete }, class: "rounded-lg px-3 py-1.5 text-sm font-medium text-slate-500 hover:bg-slate-100 hover:text-slate-900" %>
          </div>
        </div>
      </nav>
    <% end %>
    <main class="mx-auto max-w-5xl px-4 py-8">
      <% flash.each do |type, msg| %>
        <div class="mb-4 rounded-xl border px-4 py-3 text-sm <%= type.to_s == "notice" ? "border-emerald-200 bg-emerald-50 text-emerald-800" : "border-rose-200 bg-rose-50 text-rose-800" %>">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
